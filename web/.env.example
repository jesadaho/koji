# URL ของ Express (webhook + /api/liff) — dev ใช้พอร์ตเดียวกับ backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# โปรดักชัน: ถ้า reverse proxy ให้ /api ไป backend บนโดเมนเดียวกัน ใช้ค่าว่าง
# NEXT_PUBLIC_API_BASE_URL=
